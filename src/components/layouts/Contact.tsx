import { Typography } from "@mui/material";

interface IContactProps {}

const Contact = (props: IContactProps) => {
  return <Typography variant="h4">Contact panel</Typography>;
};

export default Contact;
